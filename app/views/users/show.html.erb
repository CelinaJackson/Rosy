<%= render 'layouts/header' %> 
<%= link_to "Logout", session_path, method: "delete" %> &nbsp

<h1><%= current_user.first_name %>'s Rosy Ratings</h1>

<ul> 
<% current_user.ratings.each do |rating| %> 
    <li><%= rating.wine %></li>
<% end  %> 
</ul>